@model MvcMusicDistr.Models.Gait

@{
    ViewData["Title"] = "Edit";
}

<link rel="stylesheet" type="text/css" href="/css/Scara208.css" />

<h1>Edit</h1>

<h4>Gait</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                <label asp-for="PairID" class="control-label"></label>
                <input asp-for="PairID" class="form-control" />
                <span asp-validation-for="PairID" class="text-danger"></span>
            </div>*@

            <table class="table">
                <tr>
                    <th>~</th>
                    <th>Левая</th>
                    <th>Правая</th>
                    <th>*</th>
                </tr>
                @* @foreach (var item in Model.Pairs)
                {
                    <tr class="doll2">
                        <td>
                            @Html.TextBoxFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.TextBoxFor(modelItem => item.Name)
                        </td>
                        <td>        
                            @Html.TextBoxFor(modelItem => item.OnGroundLeft)
                        </td>
                        <td>
                            @Html.TextBoxFor(modelItem => item.OnGroundRight)
                        </td>
                        
                    </tr>
                }*@
                @foreach (var item in Model.Pairs.ToList())
                {
                    <tr class="doll4">
                        <td>
                            <div class="form-group">
                                <label asp-for="@item.Id" class="control-label"></label>
                                <input asp-for="@item.Id" class="form-control" name="Id" />
                                <span asp-validation-for="@item.Id" class="text-danger"></span>
                            </div>
                        </td>
                        <td>
                            <div class="form-group">
                                <label asp-for="@item.Name" class="control-label"></label>
                                <input asp-for="@item.Name" class="form-control" name="PairName"/>
                                <span asp-validation-for="@item.Name" class="text-danger"></span>
                            </div>
                        </td>
                        <td>
                            <div class="form-group">
                                <label asp-for="@item.OnGroundLeft" class="control-label"></label>
                                <input asp-for="@item.OnGroundLeft" class="form-control" name="Left"/>
                                <span asp-validation-for="@item.OnGroundLeft" class="text-danger"></span>
                            </div>
                        </td>
                        <td>
                            <div class="form-group">
                                <label asp-for="@item.OnGroundRight" class="control-label"></label>
                                <input asp-for="@item.OnGroundRight" class="form-control" name="Right"/>
                                <span asp-validation-for="@item.OnGroundRight" class="text-danger"></span>
                            </div>
                        </td>
                    </tr>
                }
            </table>
            @ViewData["Dolls"]
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
            
            
           
        </form>

       
       
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    $(document).ready(function () {
    $("#test").click(function () {
        console.log($("#Email").val());
    });
});

}
